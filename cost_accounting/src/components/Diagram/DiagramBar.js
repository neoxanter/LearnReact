const DiagramBar = (props) => {

  let barFillHeight = '0%'

  return (
      <div>
        <div>
          < div>

          < /div>

        </div>
        <div>
          {props.label}
        </div>
      </div>
  )
}

export default DiagramBar